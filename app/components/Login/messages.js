/*
 * PersonalLogin Messages
 *
 * This contains all the text for the PersonalLogin component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PersonalLogin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PersonalLogin component!',
  },
});
